{
  "name": "ark-pro-monorepo",
  "version": "1.0.0",
  "description": "Heroku deployment wrapper for ARK Pro",
  "main": "backend/server.js",
  "scripts": {
    "heroku-postbuild": "cd backend && npm install && npm run heroku-postbuild"
  },
  "cacheDirectories": [
    "backend/node_modules",
    "backend/frontend/node_modules"
  ],
  "engines": {
    "node": "18.x"
  }
}